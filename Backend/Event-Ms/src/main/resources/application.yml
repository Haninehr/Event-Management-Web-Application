server:
  port: 8081
  

spring:
  application:
    name: event-service

  
  datasource:
    url: jdbc:mysql://localhost:3306/eventdb?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ""   

  

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true   

# ---------- JWT ----------
jwt:
  secret: 2XZCFpmx0gac8Obn7LVsXtxpT36eCKVBGEbXg3luFuN2
  expiration: 720000

# ---------- MEDIA ----------
app:
  upload-dir: ./uploads/events-media
  notification-service-url: http://localhost:8083
  notification-sevice-id : notification-service



